node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Environment / secrets — never commit
.env
.env.*
.env.local
.env.*.local
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Editor / OS
.idea/
.vscode/
*.swp

# Terraform (Phase 0+) — state and tfvars routinely contain secrets
*.tfstate
*.tfstate.*
*.tfvars
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Real-user IG export fixtures — personal data, never commit.
# (Tests skip gracefully when absent; use synthetic fixtures instead.)
apps/api/tests/parser-corpus/user_export_*.zip

# Mobile signing & cloud service credentials — never commit
google-services.json
GoogleService-Info.plist
*.keystore
credentials.json
*-key.json
service-account*.json
*.p8
*.p12
*.jks
*.key
*.mobileprovision
*.pem
